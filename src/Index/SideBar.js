function SideBar() {
  return (
    <div className="sideBar">
       <h2>SideBar</h2>
       <p>This is static sidebar for all pages</p>
       <p>We will build more on it in next iterartions.</p>
     </div>
      );

}

export default SideBar;
